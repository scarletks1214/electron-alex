// @flow
import { BILLING } from "./types";

const initialState = {};

export default function billingReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
